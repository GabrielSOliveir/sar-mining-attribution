# =============================================================================
# Pinned environment for exact reproducibility of the paper's numeric results.
#
# These versions reproduce the per-state SASE metrics of Table
# "SASE performance per state" (results/reference_res020/sase_summary.csv)
# bit-for-bit, and all reviewer-requested experiments in src/reviewer_experiments.py
# were produced with this same locked environment so that every reported number
# is mutually consistent. Verified on CPython 3.9.6 (macOS, arm64); also compatible
# with 3.11/3.12. Recreate with:
#     python -m venv .venv && ./.venv/bin/pip install -r requirements.txt
# =============================================================================

# Core modeling + analysis (pinned)
numpy==2.0.2
pandas==2.3.3
scikit-learn==1.6.1
xgboost==2.1.4
imbalanced-learn==0.12.4
scipy==1.13.1
matplotlib==3.9.4
seaborn==0.13.2

# Geospatial (spatial blocking; Figure 2 map is optional)
shapely==2.0.7
matplotlib-scalebar>=0.8      # only for the Figure 2 map
geopandas>=0.14               # only for the Figure 2 map
geobr>=0.2                    # only for the Figure 2 map

# Google Earth Engine feature extraction (only for scripts under gee/ and src/data_prep/)
earthengine-api>=0.1.400

# macOS note: XGBoost needs the OpenMP runtime. Install it with:
#     brew install libomp
# If XGBoost still fails to load libomp (no Homebrew), point the loader at the
# copy that scikit-learn already ships — no system install required:
#     export DYLD_FALLBACK_LIBRARY_PATH=$(python -c "import sklearn,os;print(os.path.join(os.path.dirname(sklearn.__file__),'.dylibs'))")
